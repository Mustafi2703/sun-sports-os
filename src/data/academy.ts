// Re-exports for helpers — live data comes from AcademyProvider + Railway API.
export {
  initialsColor,
  initialsOf,
  inr,
  type AttendanceMark,
  type Batch,
  type Coach,
  type FeeStatus,
  type Student,
  type AcademySnapshot,
} from "@/lib/api";
